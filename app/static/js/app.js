// EDGAR v2 — JavaScript applicatif (vanilla, aucun build, aucun CDN).
// Brique 1 : socle vide. Les briques suivantes ajouteront le streaming SSE,
// la sidebar repliable, le panneau de réglages de recherche, etc.
"use strict";

document.addEventListener("DOMContentLoaded", () => {
  console.info("EDGAR v2 — socle chargé.");
});
